Ana Laura 
Ejemplo de consultas DE BD


mysql -u root -p
use information_schema;
desc tables;

---
select table_name "Tabla", case when data_length/1024>1 then concat(data_length/1024," Kilob") else concat(data_length, " Bytes")
end as "Tamaño" from tables where table_schema="mysql";


--- Mostrar el tamaño promedio de las tablas, número de tablas y cantidad total de registros de las bases de datos.
 select table_schema "Base de Datos", avg(data_length+index_length) "Tamaño promedio de tablas",count(*) 
"Total de tablas",max(table_rows) "Cantidad total" from tables group by table_schema;